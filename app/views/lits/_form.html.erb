<%= form_with(model: lit, local: true) do |form| %>
  <% if lit.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(lit.errors.count, "error") %> prohibited this litter from being saved:</h2>

      <ul>
      <% lit.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>


  <p>

    <div class="row">
      <div class="col md- offset-md-3">
    <%= form.label :body%>

    </div>
    </div
    <br>
    <p>
    <%= form.text_field :body, :id => 'body', :class => 'form-control'  %>

  </p>


  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
